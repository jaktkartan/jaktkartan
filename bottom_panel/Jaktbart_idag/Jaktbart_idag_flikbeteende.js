// Jaktbart_idag_flikbeteende.js

function hämtaData() {
    // Exempel på hämtning av data (ersätt med din faktiska implementation)
    let data = "Exempel på data från JavaScript-filen.";

    // Uppdatera HTML-elementet med den hämtade datan
    let dataContainer = document.getElementById('data-container');
    dataContainer.textContent = data;
}

