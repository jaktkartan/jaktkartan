name: Convert Markdown to HTML

on:
  push:
    branches:
      - main  # Uppdatera med din huvudgrens namn här

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Install dependencies
      run: npm install  # Du kan behöva justera detta beroende på din miljö och vilka verktyg du använder

    - name: Convert Markdown to HTML
      run: |
        # Här kan du skriva kommandon för att konvertera Markdown-filer till HTML
        # Exempel: markdown-to-html ./path/to/markdown-files/*.md ./path/to/output-directory

    - name: Deploy HTML to web server
      # Här kan du skriva kommandon för att distribuera dina HTML-filer till din webbserver

